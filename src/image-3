\caption{Простой пример структуры окружений}
\label{P3.1}
\caption{Структура окружений, порождаемая вычислением {\tt (define (square x) (* x x))} в глобальном окружении.}
\label{P3.2}
\caption{Окружение, создаваемое при вычислении {\tt (square 5)} в глобальном окружении.}
\label{P3.3}
\caption{Процедурные объекты в глобальном кадре окружения.}
\label{P3.4}
\caption{Окружения, созданные при вычислении {\tt (f 5)} с использованием процедур, изображенных на рис.~\ref{P3.4}}
\label{P3.5}
\caption{Результат определения {\tt make-withdraw} в глобальном окружении.}
\label{P3.6}
\caption{Результат вычисления {\tt (define W1 (make-withdraw 100))}.}
\label{P3.7}
\caption{Окружения, создаваемые при применении процедурного объекта {\tt W1}.}
\label{P3.8}
\caption{Окружения после вызова {\tt W1}.}
\label{P3.9}
\caption{Создание второго объекта при помощи {\tt (define W2 (make-withdraw 100))}}
\label{P3.10}
\caption{Процедура {\tt sqrt} с внутренними определениями.}
\label{P3.11}
\caption{Списки {\tt x}: {\tt ((a b) c d)} и {\tt y}: {\tt (e f)}.}
\label{P3.12}
\caption{Результат применения {\tt (set-car! x y)} к спискам, изображенным на рис.~\ref{P3.12}.}
\label{P3.13}
\caption{Результат применения {\tt (define z (cons y (cdr x))} к спискам, показанным на рис.~\ref{P3.12}.}
\label{P3.14}
\caption{Результат применения {\tt (set-cdr! x y)} к спискам с рис.~\ref{P3.12}.}
\label{P3.15}
\caption{Список {\tt z1}, порождаемый выражением {\tt (cons x x)}.}
\label{P3.16}
\caption{Список z2, порождаемый выражением {\tt (cons (list 'a 'b) (list 'a 'b))}.}
\label{P3.17}
\caption{Операции над очередью.\index{ru}{очередь|операции||||} }
\label{P3.18}
\caption{Реализация очереди в виде списка с указателями на начало и конец.}
\label{P3.19}
\caption{Результат применения {\tt (insert-queue! q 'd)} к очереди с рисунка~\ref{P3.19}}
\label{P3.20}
\caption{ Результат применения {\tt (delete-queue!  q)} к очереди с рис.~\ref{P3.20}.}
\label{P3.21}
\caption{Таблица, представленная в виде списка с заголовком.}
\label{P3.22}
\caption{Двумерная таблица.}
\label{P3.23}
\caption{Элементарные функциональные элементы в имитаторе цифровых схем.}
\label{P3.24}
\caption{Полусумматор.}
\label{P3.25}
\caption{Сумматор.}
\label{P3.26}
\caption{Каскадный сумматор для $n$-битных чисел.}
\label{P3.27}
\caption{Уравнение $9C = 5(F - 32)$, выраженное в виде сети ограничений.}
\label{P3.28}
\caption{Временная диаграмма, показывающая, как чередование действий при двух операциях со счетом может привести к неправильному балансу. \index{ru}{временн\'{а}я диаграмма||timing diargam|||}\index{en}{timing diargam||временн\'{а}я диаграмма|||}}
\label{P3.29}
\caption{Одновременные операции при работе с совместным счетом в Банке~1 и личным счетом в Банке~2.}
\label{P3.30}
\caption{Решето для поиска простых чисел в виде системы обработки сигналов.}
\label{P3.31}
\caption{Процедура {\tt integral} в виде системы преобразования сигналов}
\label{P3.32}
\caption{RC-цепь и связанная с ней диаграмма потока сигналов.}
\label{P3.33}
\caption{<<Аналоговая компьютерная цепь>>, которая решает уравнение $dy/dt = f(y)$.}
\label{P3.34}
\caption{Диаграмма потока сигналов для решения линейного дифференциального уравнения второго порядка.\index{ru}{дифференциальное уравнение|второго порядка||||(упр.~3.78)}}
\label{P3.35}
\caption{Последовательная RLC-цепь}
\label{P3.36}
\caption{Диаграмма потока сигналов для решения уравнений последовательной RLC-цепи.}
\label{P3.37}
\caption{Совместный банковский счет, смоделированный через слияние двух потоков событий-транзакций.}
\label{P3.38}
